server:
  port: 9011

spring:
  application:
    name: product-service

  datasource:
    url: jdbc:mysql://mysql-service/eshop-product?useSSL=false
    username: $mysql_username
    password: $mysql_password
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true